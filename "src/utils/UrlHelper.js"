// src/utils/UrlHelper.js

class UrlHelper {
  static getRelativePath(url) {
    return url.replace(/https?:\/\/[^\/]+/, '');
  }

  static isValidHLSUrl(url) {
    const hlsRegex = /^[^:]+:(\/\/)?(hls|http(s?)|(rtsp))\//;
    return hlsRegex.test(url);
  }
}

export default UrlHelper;
